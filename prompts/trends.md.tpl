Section: {{section_title}}
Reporting period: {{period_start}} to {{period_end}}
APPROVED ANALYSIS (quote verbatim):
{{evidence_table}}
SOURCE CASE IDS: {{case_ids}}
LIMITATIONS:
{{limitations}}
NARRATION RULES:
{{narration_rules}}

Write the trends and important observations section as a concise, professional paragraph (4-7 sentences) built ONLY from the monthly evidence (trend.monthly_cases) and the period {{period_start}} to {{period_end}}.

ABSOLUTE REQUIREMENTS:
- "None" is NEVER an acceptable answer: trend.monthly_cases is non-empty in the APPROVED ANALYSIS.
- Open the paragraph with "During the reporting period {{period_start}} to {{period_end}}, case volumes ranged from <lowest-month count> to <highest-month count>." Identify the lowest and highest months <month> (<count>) from the table.
- THEN describe the notable changes: name the month with the largest increase (<month>, +<delta>), the month with the largest decrease (<month>, +/<delta>), and cite at least two more monthly figures as <month> (<count>, <+/-delta>).
- Your paragraph MUST quote at least six figures from the trend table (month counts and/or month-over-month deltas), always verbatim.
- Present volumes as observed data only. Never characterize a month or change as a safety signal, risk, or causal effect.
- If the table contains no dated cases, say explicitly "trend data was not supplied"; otherwise never write "None".
- Do not show chain-of-thought or planning. Output ONLY the finished narrative paragraph — no headings, no bullets.